<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dicoding.finalsoccermatches">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="android.support.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".presentation.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.match.detail.MatchDetailActivity"
            android:parentActivityName=".presentation.MainActivity" />
        <activity
            android:name=".presentation.search.MatchSearchActivity"
            android:parentActivityName=".presentation.MainActivity" />
        <activity
            android:name=".presentation.team.detail.TeamDetailActivity"
            android:label="@string/title_activity_team_detail"
            android:parentActivityName=".presentation.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.dicoding.finalsoccermatches.presentation.MainActivity" />
        </activity>
    </application>

</manifest>